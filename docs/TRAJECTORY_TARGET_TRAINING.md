# Complete trajectory target training

The balanced-target200 audit counted only310/748 cup FG rows and286/630 pill FG rows actually sampled. Ordinary cup/pill expert supervision consisted solely of400 frame0 occurrences per task;95/85 available noninitial ordinary rows were unused. Both mid-correction training-split observations used in the fixed diagnostic had zero actual training occurrences. These findings establish exposure gaps, not a unique cause of CF failure or evidence of fitting already-trained midpoints.

This candidate uses the unchanged prepared manifest, strongest R action policy and audited S1500 FG semantic parent with exact zero-output initialization. No continuation from the regressed balanced-target action weights. The backbone and semantic heads remain frozen; only action LoRA and the declared ERAF interfaces train, at policy LR3e-6/interface3e-5. Same full-gradient10-denoising-step first24-action target-only objective; CF teacher weight4, other target losses weight1. No Correct supervision or source-language difference loss.

Each global12 batch contains4 old-five CF teacher examples,4 complete ordinary cup/pill target trajectory examples and4 cup/pill FG examples. Ordinary and FG streams alternate tasks and visit every row of each task before reshuffling. Training and holdout remain separate.800 optimizer steps ensure at least two actual occurrences of every ordinary/FG training row for the measured manifest; admission simulates this and the terminal audit verifies every actual rank journal and exact schedule. This replaces frame0-only sampling and sampling with replacement, and increases steps; it is a combined recipe intervention, not a single-factor experiment. `initial_expert_tasks` remains the CLI's task-list field for compatibility but this objective marks rows `ordinary_target_trajectory`, never `initial_expert_anchor`.

The first run screens one ERAF+FG candidate with six ranks and final800 only. Model and optimizer remain server-only. Final training audit and per-state exposure audit precede the unchanged18-observation action diagnostic and45 closed-loop CF episodes (old5 x6, new5 x3). All29 actual prior methods remain in the comparison, including the strongest35% control. Equal-task macro and strict burger supplement remain unchanged. Videos are disabled to bound disk use; observations, actions and success semantics are unchanged. A candidate DEV lead does not prove module superiority: matched recipe controls and a subsequently frozen independent test are still required.

Admission requires the completed source trial and diagnostic, hash-bound parent/manifest/identity/comparison inputs, all six GPUs free and measured final model+optimizer sizes plus5% overhead+128MiB logs and a512MiB spare floor. The bounded single-candidate output fits current storage without deleting or compacting existing artifacts. The absolute3-hour deadline stops only owned jobs. It never shuts down the platform; user goal remains active until verified.

Run `scripts/run_robotwin_trajectory_target_trial.py --source-root <balanced-target200> --output <new candidate path> --deadline <absolute timezone timestamp>`; append `--preflight-only` for read-only admission. All modifications, tests, protocol, actual rank journals, final weights, optimizer and evaluation artifacts are retained.
