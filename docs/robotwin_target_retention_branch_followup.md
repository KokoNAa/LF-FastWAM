# 左放置、排序：补齐 CF 保持与动作分叉监督

2026-09-07。此为独立测试失败后的下一轮准备，不修改已冻结的四模型结果。上一轮 Full200 的独立 CF 为左 2/10、排序 0/10，Shared400 为 3/10、2/10。不能将这份后续代码描述为已经提高了成功率。

当前检查确认，原任务 bank_full_v2 与杯子 bank_matched 的 CF 保持池都只有右放置、汉堡和堆叠各 10 个场景。采集器及训练入口还分别硬性拒绝左放置、排序的 CF 保持数据。本次解除两处限制；原三项仍必须保留，每项至少 10 个真实成功的训练场景。数据留出场景不计入训练配额。

新采集器记录所执行指令对应的完整成功，原目标成功不能替代 CF 成功。保留实际执行控制序列与模型提出的 32 步动作块，明确区分它们；每个场景结束后保存可检查的部分结果。新相机归档与教师权重只记录路径、大小和修改时间，不扫描整文件哈希。缓存准备入口支持这些新记录；旧数据兼容原格式。

配对专家探针 `probe_robotwin_target_goal_branches.py` 读取原任务训练失败记录，从真实策略前缀重放得到相同相机图像、关节状态和物理状态，再分别完成原目标和 CF。两个分支均须经过两次控制重放验证。报告前 24/32 帧动作差异及第一个分歧帧；专家可达性不能计作模型成功率。左任务可显式选择持物放置分支，失败时如实拒绝。排序纠正时清理哪个槽位由当前选定的 RGB/BGR 目标决定。

## 先采集，不自动训练

在已启动的六卡实例执行以下命令。脚本不负责开机。输出目录必须不存在，避免覆盖上一轮。

```bash
cd /root/gpufree-data/LF-FastWAM-eraf-fg
git pull --ff-only origin codex/robotwin-eraf-fg
bash scripts/launch_robotwin_target_cf_retention.sh \
  /root/gpufree-data/LF-FastWAM/runs/robotwin_target_retention_20260907
```

六个采集进程各请求 4 个成功场景，两项目标各 12 个；每个进程最多尝试 40 个种子。使用 Shared400 教师和原两项任务的 bank_full_v2，不用杯子银行替代排序。种子分别位于 80800000/80801000/80802000 与 80810000/80811000/80812000 起始区间。本地两份现有银行无这些场景，采集时仍检查服务器实际父银行，遇到冲突即拒绝整次请求。

采集有进程级超时；整个活动仍沿用原工作截止 2026-09-07 11:30 HKT 和绝对上限 12:36:22，不因重开阶段重置。临近截止时脚本拒绝启动。六张卡必须空闲，终止时只处理自己启动的进程。`collection_result.json` 只有六个分片都实际成功退出、各有四个无重叠成功场景才会 complete=true。不足配额时保留全部日志和部分记录，不算采集完成。

## 采集成功后的缓存与训练约束

用 `prepare_robotwin_eraf_fg_replay.py worker` 对六个新的 `shard*/manifest.json` 准备缓存，基础银行使用 `bank_full_v2/manifest.json`，缓存教师使用 `grounding3000/step_002250.pt`。随后用同一工具的 `merge` 模式追加。该入口兼容新的文件元数据记录，不要求运行旧的整批哈希汇总工具。

缓存衔接已用本地集成测试覆盖真实 NPZ 读取、紧凑缓存写入/重建及合并；GPU 编码器由测试替身提供，因此这不是上机验证。新缓存分片记录输入文件元数据，合并前检查教师、父银行、采集清单、分片计数和缓存文件是否匹配；旧父银行审计不会冒充追加后的完整审计。

训练时明确指定所有五项 CF 保持任务：

```text
--cf-retention-tasks place_a2b_left blocks_ranking_rgb place_a2b_right place_burger_fries stack_blocks_two
--target-tasks place_a2b_left blocks_ranking_rgb
--policy-scope action --skip-file-hashes
```

配对探针是一项前置诊断，当前尚未实现把该新配对格式自动并入训练银行。先验证有完整成功且前 24 步确实存在动作差异，再实现对应缓存与损失；不将普通初始专家数据或共同释放动作冒称为有分歧的失败状态监督。

下一轮需要分别比较“新增目标 CF 保持”和“在此基础上加入 FG”的作用，直接使用包含左、排序 FG 的银行。ERAF 仍需针对左任务完成语义检查，再做独立开关对照。各组使用共同父权重、相同优化器预算和固定开发场景；不能把这次数据管线修复本身当作模块效果证据。

91M 两项目标的独立测试已使用，其成功轨迹不得纳入本轮训练后再标成未见测试。后续开发与最终测试需要不同的新场景，测试前固定模型；旧五任务保持要求继续适用。

## 11:49 恢复后的明确授权

2026-09-07 11:49 HKT，用户明确授权六卡最多再运行四小时，完成或到点关机。新绝对关机时间收紧为今天 15:45，平台已设置；新工作截止为 15:25，留出备份时间。此前 11:30/12:36 的上限仅因这次明确授权被替换，不修改上一轮结果。新采集命令为：

```bash
bash scripts/launch_robotwin_target_cf_retention.sh \
  /root/gpufree-data/LF-FastWAM/runs/robotwin_target_retention_20260907 \
  2026-09-07T15:25:00+08:00
```

脚本省略第二个参数时仍使用旧截止时间；不会因重新启动而自动延长。
