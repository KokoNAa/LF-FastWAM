# Initial-state anchoring trial

The expanded60 trial completed180CF episodes: fullERAF+FG25%, ERAF30%, noERAF25%, FG-only23.33%, versus historical strongest35% ten-task DEV macro. All new tasks failed. A subsequent frozen five-model diagnostic on18 deterministic train/replay-holdout observations per model found lower heldout corrective-state action MSE for fullERAF+FG (cup11.4%, pill35.6% below strongest), but larger ordinary-initial errors. These small action-fit measurements do not prove a rollout failure mechanism or success.

Actual training journals contain only7cup and14pill ordinary initial expert draws, against150corrective draws each. Correction frame zero denotes a failed-policy state, not an ordinary initial scene. Relative to the previous smaller FG bank, left/RGB received five times their nominal weighted correction exposure.

The new prespecified recipe starts all four arms afresh from the same strongest action policy and ordinary/FG semantic1500 zero-output parents as expanded60. It reuses the same data and architecture. For each of200optimizer steps, global batch12 contains2Correct retention,4CF retention,2ordinary expert,1ordinary-initial expert and3FG/matchedordinary slots. The initial slot alternates task-balanced cup/pill, then samples a training scene, always the actual same-state source/target frame-zero pair. This provides100anchor draws per task. Its existing paired flow/endpoint/conditional supervision is unchanged. The replaced random expert stream is still consumed, preserving the other draw sequences and shuffle positions.

Keep global correction coefficient1, with explicit task multipliers0.2 for left/RGB and1 for cup/pill. Apply identical multipliers to FG-off ordinary replacements. Preserve policy/interface learning rates3e-6/3e-5, CFweight4, Correctweight1, and noise seeds. Correct remains unconstrained; its old retention objective is unchanged. Sampler stratification and task weights change together, so this is a recipe comparison, not a single-factor causal attribution.

Save only final200, audit actual per-rank samples/weights/anchor flags, frozen tensors and optimizer/checkpoint binding, then run all180CF episodes with unchanged goals, catalogs and deployment. Keep all17previous methods and four archived current aliases, yielding21method comparison. No test split is opened and no selection freeze is created. A strict improvement over35% and the actual controls, with per-task regressions reported, is still required before considering independent validation.

The operator stops only its own processes on its deadline or storage reserve; it never shuts the platform down. Server-only model storage remains mandatory. Retired checkpoints may be losslessly compacted with exact tensor/metadata readback verification and persistent restoration receipts; all optimizer states and the strongest/init parents are retained.
