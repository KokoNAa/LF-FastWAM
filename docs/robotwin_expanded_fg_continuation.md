# Resume after the expanded-bank diagnostic comparison failure

The original expanded60 operator completed both 500-step semantic refreshes, their actual-sample audit and all four diagnostic workers. It then stopped because the semantic comparator required an identical training-manifest hash, although the explicitly expanded training bank retained the same 80 held-out diagnostic queries. No action optimizer steps started. Preserve the original terminal driver and traceback.

The comparator now has an explicit `allow_distinct_manifests` option. Its default remains strict. Opting in still compares every actual image/state hash, raw path/frame, instruction, clause identity and truth label. It records the separate training-manifest hashes; it does not reinterpret different diagnostic inputs as matched. The full operator opts in for this declared bank expansion.

`continue_robotwin_expanded_fg_actions.py --source-root ORIGINAL --output NEW --deadline ABSOLUTE` admits only that exact terminal failure with every expected worker exiting zero, every recorded source process gone/zombie and no pre-existing joint directory. It re-runs the actual semantic training audit, verifies hashes and fresh identity checks against both real semantic checkpoints, verifies all168 masks and reconstructs the fixed-query comparison. It records these proofs in the new output before acquiring GPUs.

The continuation reuses the completed semantic1500 models and existing identity reports. It calls the same action-stage function as the full operator: four fresh joint200 arms, unchanged sampling and losses,180CF episodes and all17development controls. It does not rerun semantics or select a different checkpoint. Only its own children are stopped at its deadline; the server remains on. The old protocol and outputs remain unchanged, while the new protocol records the original code commit and source artifact hashes.

The fixed semantic comparison is diagnostic evidence only. It cannot establish improved closed-loop CF performance or independent superiority.
